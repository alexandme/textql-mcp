# MCP SDK requirements
mcp>=1.6.0
fastapi>=0.95.0
uvicorn>=0.22.0

# Core dependencies
toml
pyyaml

# LangChain dependencies
langchain
langchain-core

# Google Cloud dependencies
google-cloud-aiplatform

# Google Spanner dependencies (optional)
# Install with: pip install -r requirements-spanner.txt
# or: pip install .[spanner]
